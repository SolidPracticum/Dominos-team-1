import { Link } from "react-router-dom";

const HomePage = () => {
    return(
        <div>
            DOMINO
            <Link to="/login">login</Link>
        </div>
    )
}

export default HomePage;